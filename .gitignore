# dependencies / build output
node_modules/
dist/
target/

# logs / OS cruft
*.log
.DS_Store
Thumbs.db

# local tooling / editor
.claude/settings.local.json
.vscode/
.idea/

# user profiles exported by the app
*.forja

# bundled Intel Wi-Fi driver (47MB) — kept out of git history; use Git LFS to track
apps/desktop/src-tauri/resources/wifi-intel.exe

# Playwright MCP scratch
.playwright-mcp/
